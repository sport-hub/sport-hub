coverage:
  status:
    project:
      default: off
      frontend:
        # target: 50%
        flags: frontend
      backend:
        # target: 50%
        flags: backend

flags:
  backend:
    paths:
      - ranking/server/
  frontend:
    paths:
      - ranking/client/

comment:
  layout: "reach, diff, flags, files"
  behavior: default
