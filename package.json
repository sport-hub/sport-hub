{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "pretest": "eslint packages",
    "test": "jest"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "dependencies": {
    "es6-promise-pool": "^2.5.0"
  }
}
